apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: pacman-eks
  region: eu-central-1
  version: "1.33"

autoModeConfig:
  enabled: true
  nodePools:
    - general-purpose
    - system
